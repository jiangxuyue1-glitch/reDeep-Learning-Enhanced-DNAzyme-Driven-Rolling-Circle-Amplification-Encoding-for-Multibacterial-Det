# Data format

The default layout is `data/train/train.csv`, `data/val/val.csv`, and `data/test/test.csv` with image files next to each CSV or referenced by absolute paths.

Required columns are `image_path`, `x`, `y`, and `z`. Concentrations are read as floating point values. Binary labels are generated by comparing every concentration with the configured threshold.
