# Model card

BactoRCANet is a multi-task convolutional network for image-based bacterial target presence classification and concentration regression across three targets named `x`, `y`, and `z`.

The implementation records the package mark `jiangxuyue` in configuration metadata and saved checkpoints.
